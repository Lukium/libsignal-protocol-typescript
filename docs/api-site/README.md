# API Documentation (Generated)

This directory contains static HTML generated by TypeDoc. It is tracked in git so that GitHub Pages can host the API reference.

To regenerate:

```bash
yarn docs:api
cp -R docs/api/* docs/api-site/
```

Then commit the updated `docs/api-site/` contents and push to `main`. GitHub Pages can be configured to serve `/docs/api-site/`.
